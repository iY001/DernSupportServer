{
  "version": 2,
  "builds": [
    { "src": "index.js", "use": "@vercel/node" }
  ],
  "routes": [
    { "handle": "filesystem" },
    { "src": "/(.*)", "dest": "index.js" }
  ],
  "scripts" :{
    "build": "prisma generate && npm run build"
  }
}